name: 🐛 Bug Report
description: Report a bug or unexpected behavior
title: "[BUG] "
labels: ["bug", "needs-triage"]
body:
  - type: dropdown
    id: package
    attributes:
      label: 📦 Package
      options:
        - hermes-api (Backend/API)
        - hermes-app (Frontend/UI)
        - docs (Documentation)
        - devops (Docker/Deployment)
        - other
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: 🐛 What happened?
      description: Describe the bug and what you expected
      placeholder: |
        What I did:

        What I expected:

        What actually happened:
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: 🔄 Steps to reproduce
      placeholder: |
        1.
        2.
        3.
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 🌍 Environment (optional)
      placeholder: |
        - OS:
        - Deployment: Docker / Manual / Other
        - Version:
    validations:
      required: false
